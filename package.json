{
  "name": "eslint-comments-rule",
  "version": "0.0.1",
  "description": "Custom eslint rules for enforce code comments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "keywords": ["eslint", "eslint-plugin", "eslint-rule", "eslint-comment", "eslint-rule-comment"],
  "author": "hi_mochy",
  "license": "ISC",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/eslint": "^9.6.1",
    "@types/micromatch": "^4.0.9",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "micromatch": "^4.0.8"
  },
  "eslintConfig": {
    "plugins": ["custom"],
    "rules": {
      "comment-prefix": "error"
    }
  }
}
